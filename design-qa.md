# Design QA

## 对照目标与证据

- 参考视觉真值：`artifacts/design-qa/source-guided-dialogue.png`
- 桌面实现截图：`artifacts/design-qa/implementation-desktop.jpg`
- 响应式实现截图：`artifacts/design-qa/implementation-mobile.jpg`
- 同屏对照证据：`artifacts/design-qa/combined-comparison.jpg`
- 参考图像素：1487 × 1058。
- 桌面实现图像素：1173 × 720；浏览器桌面内容区截图，1× 输出。
- 响应式实现图像素：1173 × 720；浏览器请求 390 × 844 CSS px 的窄屏配置后，由 Codex 浏览器表面输出的缩放截图，只用于判断重排、换行、遮挡与触控区域，不用于逐像素密度比对。
- 归一化方式：同屏对照使用固定 1173 × 720 画布，将参考图和桌面实现分别按 `object-fit: contain` 等比例缩放到两个等宽区域；不比较浏览器外框、画布空白或密度差异。
- 实现状态：Part 1「认识同事」，第二遍「扮演角色 A」，第 1 / 4 轮，答案隐藏，口语操作栏可见。

## Findings

- 没有剩余的 P0、P1 或 P2 问题。
- 字体与排版：参考稿和实现都采用清晰的无衬线层级；实现保留大标题、辅助说明、英文句子和中文意图四级层次。中文内容带来的字宽差异属于产品本地化需要，没有截断或难读的压缩。
- 间距与布局：实现保留参考稿的「左侧对话主区 + 右侧口语教练 + 底部语音操作栏」结构，并将三遍练习进度置于首屏。桌面区域比例、卡片间距和底部固定栏都稳定；窄屏检查未见重叠、横向溢出或主导航被遮挡。
- 颜色与视觉令牌：奶油背景、陶土主色、苔藓绿提示和低对比边线与参考方向一致；无渐变。状态色仍能区分当前轮次、教练提示和主要操作。
- 图像与资产：该界面不依赖照片、插画或品牌图像；所有功能图标均使用本地 Phosphor SVG 资产，没有 emoji、文本符号图标、内联 SVG 或用于冒充资产的 CSS 图形。
- 文案与内容：实现将参考稿中的示例文本替换为零基础中文引导，并只展示 Part 1 已批准词语。改动属于需求约束，不是视觉漂移。
- 交互与无障碍：开始练习、三遍流程、显示答案、慢速播放、手动自评、底部导航可操作；Escape 能退出练习；当前对话轮次有明确键盘焦点；本地浏览器控制台错误为 0。

## 同屏与局部判断

- 全景证据：`artifacts/design-qa/combined-comparison.jpg` 同时显示参考稿与最终角色 A 页面，可直接判断信息层级、主辅栏比例、底部操作区和整体色彩。
- 局部证据：不需要额外裁切。最终同屏图中标题、进度、当前英文句子、教练任务、词语标签、底部操作和导航均可辨认；重要图标、输入控件或密集表格没有小到无法判断的区域。

## Comparison history

1. 第一轮发现 P2：部分控制仍使用 `▶`、`‹`、`›` 等文本字形表达图标；窄屏播放器控制轨道也过于依赖固定列宽。修复为明确的中文文字操作，并将窄屏操作区改成弹性双列布局。修复后重新渲染，证据见桌面与响应式实现截图。
2. 第二轮发现 P2：页面背景和首屏卡片存在参考稿没有的 CSS 装饰形状，降低了与参考视觉的克制度一致性。删除这些装饰伪元素及其动画后，重新捕获桌面、窄屏和同屏对照。最终证据见 `artifacts/design-qa/combined-comparison.jpg` 和 `artifacts/design-qa/implementation-mobile.jpg`。
3. 最终复核：没有可执行的 P0、P1 或 P2 差异；没有需要阻塞发布的 P3 项。

## 实现检查清单

- [x] 保留参考稿的口语主区、教练侧栏和固定操作区。
- [x] 三遍练习状态可见且可交互。
- [x] 仅使用当前 Part 的已批准词语。
- [x] 本地模式、慢速朗读与手动自评无需 API。
- [x] 窄屏布局无横向溢出或关键控件遮挡。
- [x] 键盘退出和焦点样式可用。
- [x] 浏览器控制台无错误。

final result: passed
